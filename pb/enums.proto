syntax = "proto3";

package stredono;

option go_package="github.com/pkulik0/stredono/cloud/pb";

enum Currency {
  UNKNOWN = 0;
  PLN = 1;
}

enum Tier {
  BASIC = 0;
  PLUS = 1;
}

enum AuthLevel {
  NONE = 0;
  EMAIL = 1;
  OIDC = 2;
}